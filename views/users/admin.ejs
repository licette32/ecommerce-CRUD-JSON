<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Panel de Administración - Caña&Carnada</title>
  <link rel="stylesheet" href="/css/admin.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('../partials/adminHeader') %>

  <main>
    <div class="container d-flex justify-content-between align-items-center mt-4">
      <h1>Bienvenido al Panel de Administración</h1>
      <a href="/productos" class="btn btn-outline-light bg-dark">
        <i class="btn-back-to-shop"></i> Ver Productos
      </a>
    </div>
    <p class="container">Desde aquí puedes gestionar los productos de la tienda.</p>

    <section class="admin-section container">
      <a href="/admin/productos/create" class="btn-create-product">Crear nuevo producto</a>

      <ul class="admin-product-list">
        <% products.forEach(producto => { %>
            <li class="admin-product-item">
            <img src="/<%= producto.imagen %>" alt="Imagen de <%= producto.nombre %>" class="admin-product-img">
            <div class="admin-product-info">
                <h3><%= producto.nombre %></h3>
                <p>Precio: $<%= producto.precio %></p>
                <div class="admin-product-actions">
                <a href="/admin/productos/edit/<%= producto.id %>" class="admin-btn-edit">Editar</a>
                <form action="/admin/productos/<%= producto.id %>?_method=DELETE" method="POST" class="form-delete">
                    <button type="submit" class="admin-btn-delete" onclick="return confirm('¿Seguro que deseas eliminar este producto?');">Eliminar</button>
                </form>
                </div>
            </div>
            </li>
        <% }) %>
      </ul>
    </section>
  </main>
</body>
</html>